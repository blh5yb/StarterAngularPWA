import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'login-start-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
